{
  "installCommand": "npm install && npm install --prefix biotina",
  "buildCommand": "npm run build",
  "outputDirectory": "biotina/dist",
  "functions": {
    "api/health.ts": {
      "maxDuration": 10
    },
    "api/pix/charge.ts": {
      "maxDuration": 60
    },
    "api/order/[orderId].ts": {
      "maxDuration": 30
    }
  }
}
